# Workspace Custom Rules

## Commit Guidelines
- **Language**: All commit messages must be written in English.
- **Format**: Use bullet points for commit descriptions to make them clear and readable.
- **Approval Required**: Whenever the user asks to generate a commit description or make a commit, ALWAYS present the proposed commit messages to the user first and ask for their explicit approval before executing any `git commit` commands. Do not execute them automatically.
